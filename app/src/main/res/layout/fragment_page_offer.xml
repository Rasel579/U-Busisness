<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@color/bridal_heath">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <include
            android:id="@+id/offerPage_toolbar"
            layout="@layout/base_page_toolbar" />

        <TextView
            android:id="@+id/offerPage_text"
            tools:text="@string/dummy_text"
            style="@style/base_page_text_style" />

        <Button
            android:id="@+id/offerPage_btn"
            android:text="@string/more"
            style="@style/base_button_style" />

    </LinearLayout>
</ScrollView>